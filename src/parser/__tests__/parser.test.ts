describe("parser", () => {
  it("should return a list of parsers", () => {});
  it("should set the selected parser", () => {});
  it("should throw an error if selected parser does not exist", () => {});
  it("should parse homebank csv correctly", () => {});
  it("should throw an error if csv cannot be parsed", () => {});
});
